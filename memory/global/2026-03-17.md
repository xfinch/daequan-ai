# Tuesday, March 17th, 2026

## Morning — Database Sync Architecture Decision

**08:47 AM** — Xavier confirmed **Option 1**: Restore 3-layer sync architecture

**The Problem:**
- GHL has 21 current prospects ✅
- SQLite has partial data ⚠️
- MongoDB has old data (~10 records from weeks ago) ❌
- Cloud map (daequanai.com) shows stale data

**The Fix (Option 1):**
- Export GHL prospects → bulk insert to MongoDB
- Restore GHL ↔ SQLite ↔ MongoDB synchronization
- Set up ongoing sync to prevent future drift

**Executing now.**

**08:50 AM** — Reviewed existing architecture:
- **Plaud webhook** (`plaud-webhook/actions/comcast.js`) already uses MongoDB (`COMCAST_MONGODB_URI` → `comcast_crm.contacts`)
- **Main app** (`lib/db.ts`) uses MongoDB via Mongoose (`visits` collection)
- **Express legacy** also connects to MongoDB for real-time features

**09:00 AM** — Created `sync-ghl-to-mongodb.js` for main app `visits` collection
- Discovered you already have MongoDB infrastructure via Railway
- Need connection string to run sync

**11:20 AM** — Retrieved Railway credentials from plist:
- RAILWAY_CLIENT_ID: `rlwy_oaci_bURelhmjTLEn8hGLAQ8CXTny`
- MONGO_URL found: `mongodb://mongo:***@metro.proxy.rlwy.net:33745`
- Project: `balanced-miracle` (abd953e3-ae34-4e5c-9a6d-8a6bff0521c8)
- Service: `daequan-ai` (c8568c23-f757-4080-a596-5984d98d189f)

**11:22 AM** — GHL → MongoDB sync completed
- 250 contacts processed (24 added, 226 updated)
- Issue: No tag filtering — pulled all 250 GHL contacts, not just Comcast prospects
- Fixed script to filter by `comcast-prospect` tag for future syncs
- Deleted incorrect `sync-ghl-to-supabase.js` (Supabase never deployed to Railway)
- MongoDB now current with GHL data

---

## Comcast CRM Data Architecture (Documented)

**Confirmed 3-Layer Sync:**

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│      GHL        │←───→│     SQLite      │←───→│     MongoDB     │
│  (GoHighLevel)  │     │  (Mac mini)     │     │  (Railway)      │
│                 │     │  comcast.db     │     │  visits coll    │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ • 250 contacts  │     │ • Local cache   │     │ • Web map data  │
│ • Source of     │     │ • GHL sync      │     │ • daequanai.com │
│   truth         │     │   logic         │     │ • API source    │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

**Sync Scripts:**
| Direction | Script | Location |
|-----------|--------|----------|
| GHL ↔ SQLite | `ghl_sync.py` | `comcast-crm/` |
| GHL → MongoDB | `sync-ghl-to-mongodb.js` | `comcast-crm/` |

**Railway Environment:**
- `MONGO_URL` — Primary database (web app)
- No Supabase variables (Supabase map never deployed)

**Key Insight:** `daequan-map-app/` folder contains unused Supabase code. Live app uses MongoDB via `lib/db.ts`.

---

## Afternoon — Field Visit Updates (5:30 PM)

**Proctor Art Gallery / Pamela Phelps:**
- Stopped by — she wasn't in, met gatekeeper at desk (forgot name)
- Billing successfully corrected, credits in progress
- **MAJOR DISCOVERY:** She's overpaying for BVE — can adjust for lower bill
- Status changed: "Just helped" → **PROSPECT**
- ✅ GHL task created: Follow up by tomorrow (3/18)

**Smart PNW / James Roddy:**
- ✅ **COMMITTED:** Moving forward with service transition in April
- Service: 7 mobile lines (3 iPads + 4 cellphones) + BVE
- Quoted: ~$235/month
- Also wants fiber if can match current $440/month (minus $130 discounts)
- **REFERRAL PARTNER ACTIVATED:** James qualifies — can get free internet via bill credits
- **REFERRAL PROVIDED:** Chris Brown (attorney, home office Tumwater)

**NEW PROSPECT — Chris Brown (Attorney):**
- Source: James Roddy referral
- Address: 708 South 7th Ave SW, Tumwater, WA 98512
- Need: Fiber internet for home office
- Requires: Manager approval (residential/business hybrid)
- Kanban updated, pipeline entry added
